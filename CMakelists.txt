cmake_minimum_required(VERSION 3.9.1)

project(BombCat)

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall")

include_directories("${CMAKE_CURRENT_SOURCE_DIR}"
                    "${CMAKE_CURRENT_SOURCE_DIR}/cards")

add_library(bomb_cat game.cpp player.cpp card.cpp 
            
            cards/bomb.cpp cards/dismantle.cpp cards/rest.cpp)

add_executable(game_start main.cpp)
target_link_libraries(game_start bomb_cat)